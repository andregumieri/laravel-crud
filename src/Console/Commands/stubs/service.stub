<?php

namespace {{ namespace }};

use {{modelUsePath}};
use {{repositoryUsePath}};


class {{ class }}
{
 	public function __construct(
 		private readonly {{repositoryClass}} $repository
 	) {}

    public function handle(array $data): {{modelClass}}
    {
        return $this->repository->{{action}}($data);
    }
}
