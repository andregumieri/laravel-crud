<?php

namespace {{ namespace }};

use {{modelUsePath}};
use {{repositoryUsePath}};
use {{requestNamespace}};
use {{ rootNamespace }}Collections\{{modelClass}}Collection


class {{ class }}
{
 	public function __construct(
 		private readonly {{repositoryClass}} $repository
 	) {}

    public function handle({{requestClass}} $request): {{modelClass}}Collection
    {
        return $this->repository->{{action}}($request->only([]));
    }
}
